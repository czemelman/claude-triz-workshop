{
  "name": "triz-workshop",
  "version": "0.1.0",
  "description": "TRIZ matrix workshop: turn a contradiction into ranked solution candidates by selecting the right TRIZ matrix from a heterogeneous corpus, mapping the problem onto its parameters, looking up principles, interpreting them, and synthesizing/critiquing candidates. State machine in Python; LLM is a dispatcher.",
  "author": {
    "name": "TRIZ Workshop Authors",
    "email": "noreply@example.invalid"
  },
  "compatibility": {
    "matrix_collection_schema": ">=1.1.0 <2.0.0",
    "claude_code": ">=2.1.0",
    "python": ">=3.10"
  },
  "supported_artifact_schema_versions": [1],
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/validate_artifact.py --hook-mode >> .triz/hook.log 2>&1"
          }
        ]
      }
    ]
  }
}
